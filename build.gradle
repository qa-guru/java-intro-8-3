plugins {
    id 'java'
}

repositories {
    mavenCentral()
}

dependencies {
    implementation(
            'com.codeborne:selenide:6.0.1',
    )
    testImplementation(
            'org.junit.jupiter:junit-jupiter:5.6.0',
    )
    testRuntimeOnly('org.junit.jupiter:junit-jupiter-engine:5.6.0')
}

test {
    useJUnitPlatform()
}